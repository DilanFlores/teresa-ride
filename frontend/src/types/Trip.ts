export interface Trip {
  id_trip: string;
  origin: string;
  destination: string;
  price: number;
  start_date: string;   
  final_date: string;   
  people_count: number;
  description: string;
  image: string;
}
